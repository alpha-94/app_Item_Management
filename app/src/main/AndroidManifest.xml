<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.arslooper.item_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Item_App"
        tools:targetApi="m">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".item.AnalyzeActivity">

        </activity>

        <activity android:name=".item.SearchActivity">

        </activity>

        <activity android:name=".item.ConfirmActivity">

        </activity>

        <activity android:name=".entry.ConfirmActivity">

        </activity>

        <activity android:name=".entry.SearchActivity">

        </activity>

        <activity android:name=".entry.SelectActivity">

        </activity>
    </application>

</manifest>